# 디자인 원칙 (DESIGN.md)

> NHN Cloud 디자인시스템의 **주관적 판단 기준**을 정리한 문서.
> `CLAUDE.md`가 "토큰만 써라"라는 **규칙(what)**을 정의한다면, 이 문서는
> "어떤 토큰을 언제 쓰는가"라는 **판단(when/why)**을 정의한다.
> 값의 강제(하드코딩 차단)는 여전히 토큰 규칙·hook이 담당한다 — 이 문서는 그 위에 얹히는 취향 계약이다.

---

## 1. 브랜드 성격

**한마디: 신뢰·전문 (엔터프라이즈).** 감성보다 정보 밀도와 일관성이 우선한다.

| 축 | 지향 | 지양 |
|----|------|------|
| 톤 | 차분하고 정돈됨, 예측 가능함 | 튀거나 장식적, 감성 과잉 |
| 키워드 | **신뢰 · 안정 · 명료** | 화려함, 트렌디함, 재미 |
| 참고 지향 | AWS 콘솔 · Azure Portal 같은 **클라우드 관리 콘솔** | 랜딩페이지형 마케팅 UI |

- 화면의 주인공은 **콘텐츠(데이터·리소스)**이지 UI 장식이 아니다.
- 같은 요소는 어디서나 같게 보인다 — 일관성이 개성보다 우선한다.
- "예뻐 보이려고" 규칙을 깨지 않는다. 애매하면 더 조용한 쪽을 택한다.

---

## 2. 색상 사용 맥락

### brand-primary는 **액션 전용 — 엄격히 아낀다**

`brand-primary`(= `blue-700`, 토큰상 `interactive-primary` / `fill-primary-01` /
`text-primary` / `border-primary` / `icon-accent`)는 **사용자가 무언가를 할 수 있는 곳**에만 쓴다.

**써도 되는 곳 (액션):**
- Primary 버튼 (`fill-primary-01`, hover `interactive-hover` = `blue-800`)
- 활성 링크·탭·현재 위치 (`text-primary`, `border-primary`)
- 포커스 링, 선택 상태, 체크된 컨트롤 (`icon-accent`)
- 강조 아이콘 (`icon-accent`)

**쓰지 않는 곳:**
- **한 화면의 Primary 버튼은 원칙적으로 1개.** 나머지 액션은 뉴트럴(secondary) 버튼으로.
- 배경 대면적 채색 금지 — primary를 넓은 면에 깔지 않는다.
- 단순 강조·헤더 배경·장식 목적으로 쓰지 않는다. 강조가 필요하면 타이포 굵기·뉴트럴 대비로 해결한다.

### 나머지 색 역할

| 목적 | 토큰 | 비고 |
|------|------|------|
| 본문/배경/보더 | `neutral-*` 계열 (`text-*`, `bg-*`, `border-*`) | 화면의 90% 이상은 뉴트럴이어야 정상 |
| 상태 표시 | `status-critical`(red) / `status-success`(green) / `status-urgent`(purple) / `status-info`(blue-400) | **의미가 있을 때만.** 장식용 색으로 전용 금지 |
| 고정색 | `static-white` / `static-black` | 테마 불변 지점에만 |

> 색으로 위계를 만들기 전에 **뉴트럴 명도 대비**와 **타이포**로 먼저 만든다.
> primary와 status 색이 한 화면에 많이 보이면 대개 과용이다.

---

## 3. 스페이싱 철학

**8그리드 3단계.** 간격은 "무엇 사이의 간격인가"로 스케일을 고른다 — 안쪽일수록 좁고, 바깥일수록 넓다.

| 층위 | 기준 스케일 | 유틸리티 예 | 의미 |
|------|-------------|-------------|------|
| **컴포넌트 내부** (밀집) | `8` · `12` · `16` | `p-16`, `gap-8` | 라벨-아이콘, 버튼 안쪽 등 붙어야 할 관계 |
| **컴포넌트 간** | `16` · `24` | `gap-24` | 폼 필드끼리, 카드끼리 |
| **섹션 간** | `32` · `40`+ | `mt-40` | 화면 블록 구분 |

원칙:
- **간격 = 관계의 표현.** 가까운 것은 붙이고, 다른 그룹은 띄운다. 균일한 간격으로 뭉개지 않는다.
- 8배수 그리드(`8·12·16·24·32·40`)를 기본 리듬으로 삼는다.
- **비그리드 raw 토큰(`9·18·28·33·45·60·72·80·117·156` 등)은 예외다.**
  Figma 프레임 실측에서 유래한 값이며, **해당 컴포넌트의 고정 치수로만** 쓴다.
  새 레이아웃 간격을 이 값들로 지어내지 않는다 — 간격은 8그리드에서 고른다.
- 픽셀 마진으로 밀어내지 않는다. 신축은 `flex-1`·`justify-between`으로 (레이아웃 규칙 참조).

---

## 4. 컴포넌트 조합 규칙

- **Card 중첩 금지.** Card 안에 Card를 넣지 않는다. 내부 구획이 필요하면 `Divider`나
  뉴트럴 배경(`bg-surface`/`bg-subtle`) 차이로 나눈다.
- **면(Surface) 위계는 그림자가 아니라 보더·배경으로 표현한다.**
  카드·패널 경계는 `border-*` 토큰으로 구분한다 (그림자는 Modal 전용, §5 참조).
- **한 컨테이너 = 한 Primary 액션.** 여러 강조 액션을 나란히 두지 않는다.
- **상태 색은 상태 컴포넌트에만.** Badge/Alert 등 의미 전달 목적에만 status 색을 쓴다.
- 기존 컴포넌트를 먼저 재사용한다. 변형이 필요하면 새로 만들기 전에 기존 것의 prop 확장을
  검토한다 (CLAUDE.md ② Simplicity First).
- 라운드는 토큰(`radius-2·8·18·30`)에서 고르되 컴포넌트 성격에 맞춘다 —
  임의 라운드(`rounded-[…]`)로 과장하지 않는다.

---

## 5. 쓰지 말 것

플랫(flat)을 유지한다. 깊이감은 색·간격·보더로만 만든다.

| 금지 | 이유 | 대신 |
|------|------|------|
| **그라데이션** (linear/radial) | 토큰 시스템에 그라데이션 값이 없다. 엔터프라이즈 톤과 배치. | 단색 토큰 |
| **텍스트 그림자 · 발광(glow)/네온** | 정보 가독성을 해치고 장식적. | 색 대비·타이포 굵기 |
| **임의 그림자 (`shadow-modal` 외)** | elevation 가이드가 없다. 새 box-shadow를 지어내지 않는다. | 보더(`border-*`)·배경(`bg-surface`) |

> 유일하게 정의된 그림자는 `shadow-modal`(Modal 다이얼로그 전용)이다.
> 카드·드롭다운 등 다른 곳에 shadow를 임의로 붙이지 않는다 — 필요하면 `/sync-tokens`로 토큰을 먼저 추가한다.

추가로 지양:
- 과도한 애니메이션·이모지 장식 (콘솔 톤과 배치).
- 색만으로 의미를 전달하는 것 (a11y — 아이콘·텍스트를 병행).

---

## 관련 문서
- `CLAUDE.md` — 토큰 규칙·4원칙·강제 계약(hook)
- `.claude/rules/layout-components.md` — 페이지 레벨 컴포넌트 반응형 규칙
- `src/tokens/` — 모든 시각 값의 SSOT
