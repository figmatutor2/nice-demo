# 디자인 시스템 하네스

> Figma 디자인 시스템 → 코드 변환 하네스.
> 스택 전제: Vite 6 · React 19 · TypeScript 5 · Tailwind CSS v4 · Storybook 8 (addon-designs + autodocs)

## 목적과 완료 기준 (측정 가능)

이 하네스는 아래 3가지를 달성하면 성공이다. 각 항목은 측정 가능한 기준으로만 판정한다.

### 1. 일관성 보장
모든 시각 값이 디자인 토큰만 참조하게 만든다.
- **완료 기준:** 소스 전체에서 raw hex(`#fff`), raw px(`16px`), `rgb()/rgba()`,
  arbitrary Tailwind(`bg-[#...]`, `p-[13px]`) 발생 건수 = **0건**.
- **측정 방법:** 정적 스캔으로 위반 건수 집계 → 0이어야 통과.

### 2. 하드코딩 자동 차단
토큰 외 값 유입을 사람 리뷰가 아니라 도구 레벨에서 자동으로 막는다.
- **완료 기준:** 토큰에 없는 시각 값이 입력/커밋되면 **도구가 자동 차단**하여
  변경이 반영되지 않는다. 사람 리뷰 통과 여부와 무관하게 차단된다.
- **측정 방법:** 위반 값을 일부러 넣은 케이스가 도구 단계에서 non-zero exit로
  거부되는지 확인 → 거부되어야 통과.

### 3. 구현 프로세스 표준화
모든 컴포넌트 작업이 동일한 4단계 절차를 거치게 한다.
- **완료 기준:** 신규/수정된 컴포넌트의 **100%**가 정의된 4단계 절차를 따른다.
- **측정 방법:** 컴포넌트별로 4단계 산출물 존재 여부 확인 → 전부 존재해야 통과.
- *(4단계의 구체 정의는 이후 청크에서 확정)*

---

## 카파시 4원칙

각 원칙은 "막는 문제 + 강제 방식"을 쌍으로 둔다. 슬로건이 아니라 실행 규칙이다.

### 1. Think Before Coding
- **막는 문제:** 잘못된 가정 위에 코드를 쌓는 것.
- **강제 방식:** 요구사항이 모호하면 코드를 쓰지 말고 멈추고 질문한다.
  추측을 사실처럼 단정하지 않는다 — 불확실한 것은 불확실하다고 명시한다.

### 2. Simplicity First
- **막는 문제:** 부풀리기(over-engineering).
- **강제 방식:** 새로 만들기 전에 기존 토큰/컴포넌트를 먼저 재사용한다.
  요청하지 않은 추상화·설정 옵션·범용화를 추가하지 않는다.

### 3. Surgical Changes
- **막는 문제:** 범위 밖 변경.
- **강제 방식:** 변경된 모든 줄이 요청과 1:1로 추적되어야 한다.
  요청과 무관한 리팩터·포맷팅·정리를 같은 작업에 섞지 않는다.

### 4. Goal-Driven Execution
- **막는 문제:** 미완 상태로 종료.
- **강제 방식:** 시작 시 측정 가능한 완료 조건을 정의하고,
  자체 검증(스캔·타입체크·차단 도구)을 통과하기 전에는 "완료"라고 말하지 않는다.

## 표준 워크플로

모든 작업은 다음 4단계를 순서대로 거친다. 각 단계는 원칙 1~4와 1:1로 대응한다.

| 단계 | 대응 원칙 | 하는 일 |
|------|-----------|---------|
| **1. Clarify**   | ① Think Before Coding   | 모호함 해소, 가정 검증, 필요 시 질문 |
| **2. Reuse**     | ② Simplicity First      | 기존 토큰/컴포넌트 탐색·재사용 우선 |
| **3. Implement** | ③ Surgical Changes      | 요청과 1:1 추적되는 최소 변경만 |
| **4. Evaluate**  | ④ Goal-Driven Execution | 완료 조건 대비 자체 검증 후 종료 |

## 강제 계약 — 3중 레이어

이 4원칙은 문서상의 권고가 아니라 **계약**이다. 아래 3중 레이어로 강제되며,
어느 한 레이어라도 통과하지 못하면 작업은 완료로 간주되지 않는다.

| 레이어 | 이름 | 강제 주체 | 역할 |
|--------|------|-----------|------|
| **레이어 1** | 선언 | `CLAUDE.md` | 원칙·규칙·완료 기준을 선언한다. (이 문서) |
| **레이어 2** | 절차 | 에이전트   | Clarify→Reuse→Implement→Evaluate 절차로 원칙을 실행한다. |
| **레이어 3** | 자동차단 | hook     | 토큰 외 값을 도구 레벨에서 차단한다(사람 리뷰 무관). |

- 레이어 1은 "무엇이 옳은가"를 정의하고, 레이어 2는 "그것을 어떻게 실행하는가"를,
  레이어 3은 "실행이 규칙을 어기면 무엇이 막는가"를 담당한다.
- 레이어 3은 사람의 승인 여부와 독립적으로 작동한다 → 규칙 위반은 리뷰가 아니라 도구가 거부한다.

## 토큰 규칙

모든 시각 값(색·간격·타이포·라운드·그림자 등)은 **`src/tokens`에 정의된 토큰만** 참조한다.

- **금지:** raw hex(`#fff`), raw px(`16px`), `rgb()/rgba()`, arbitrary Tailwind(`bg-[#...]`, `p-[13px]`).
- **허용:** `src/tokens`에서 파생된 토큰/유틸리티만.
- 이 규칙은 레이어 3 hook에 의해 도구 레벨에서 자동 차단된다(다음 청크에서 구현).

## 에이전트 라우팅

작업 유형에 따라 담당 에이전트로 라우팅한다. 진입은 스킬(슬래시 커맨드)로 하며, 스킬이 해당 에이전트를 결정적으로 호출한다.

| 작업 | 진입 스킬 | 담당 에이전트 |
|------|-----------|---------------|
| Figma 링크/노드 → 컴포넌트 구현 | `/figma-to-code` | `figma-implementer` |
| Figma 없이 토큰만으로 컴포넌트/변형 생성 | `/new-component` | `component-builder` |
| Figma 변수 동기화 / 하드코딩 → 토큰 승격 (토큰 파일만 편집) | `/sync-tokens` | `token-guardian` |
| 완료 전 검증 게이트 (PASS/FAIL, 코드 미수정) | `/review-design` | `design-reviewer` |

## 스킬 가이드

**에이전트 = 역할(누가), 스킬 = 절차(어떻게).** 에이전트는 책임 범위를 갖고,
스킬은 그 안에서 따르는 단계별 절차를 제공한다. 각 스킬은 Clarify→Reuse→Implement→Evaluate 절차로 해당 에이전트를 호출한다.

| 스킬 | 제공 절차 | 호출 에이전트 |
|------|-----------|----------------|
| `/figma-to-code`  | Figma MCP 직접 읽기 → 토큰 매핑 → 구현 → 검증 위임 | `figma-implementer` |
| `/new-component`  | 명세 확정 → 토큰/컴포넌트 재사용 → surgical 구현 → 검증 위임 | `component-builder` |
| `/sync-tokens`    | 값 분류 → Figma 변수 확인 → 토큰 파일만 편집 → 매핑 표 | `token-guardian` |
| `/review-design`  | 6개 게이트(하드코딩0·토큰·범위·빌드·a11y·스크린샷) 점검 → PASS/FAIL | `design-reviewer` |
| `/description-guide` | 양식 확인 → 디자인 판독 → md 정리 → DescriptionGuide 스토리 생성 | (스킬 자체 수행) |
